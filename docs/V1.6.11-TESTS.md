# Methodz Meeting Manager v1.6.11 Test Plan

## Portable review core

Run:

```bash
node tests/v1611-meeting-review.mjs
```

Coverage includes strict date-only validation, overdue and due-soon boundaries, unassigned and invalid-date detection, deterministic severity ordering, bounded output, attention filtering, live-pulse completion, next-section selection, and proof that derivation does not mutate source records.

## Browser workflow

Run:

```bash
npx playwright test tests/v1611-follow-up-review.spec.js --reporter=line
```

Coverage includes shell `1.6.11` with record schema `1.6.0`, valid panel-registry registration, Meeting-Day navigation, saved-record task review, explicit source-meeting opening, read-only pulse behavior, focus transfer to the next incomplete section, and narrow-phone overflow protection.

## Regression gate

Before merge, retain all existing signing, recovery, custody, provider, synchronization, transfer, acceptance, rollback, mobile, panel-registry, direct-file, and browser checks. The service worker must remain static-only, and no production endpoint or credential may appear in the deployed shell.
