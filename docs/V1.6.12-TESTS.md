# v1.6.12 Test Plan

## Portable core

`node tests/v1612-workspace-capacity.mjs`

Covers UTF-8 byte estimation with one reused encoder, stable category aggregation, archive-first classification for the real overlapping archive key, quota utilization, threshold behavior, deterministic sorted selection, capped value reads and raw snapshot entries, accurate complete entry counts and truncation, enumeration and value-read failures as metadata-safe unavailable reports, sensitive-data and raw-error exclusion, bounded synthetic workloads, deterministic timing, review truncation, metadata-only reports, and incompatible-core rejection.

## Chromium workflow

`npx playwright test tests/v1612-workspace-capacity.spec.js`

Covers explicit capacity execution, configured-limit enforcement during browser snapshot collection, accurate total-entry and truncation reporting, an explicit unavailable UI/report when a localStorage value read fails, no healthy zero-entry or partial result after failure, no raw error leakage, real archive storage rendered only as **Archive Vault**, no browser-local mutation or draft autosave from capacity controls, no sensitive text or key names in rendered output, bounded synthetic execution, no synthetic persistence, narrow-phone layout, and touch targets.

## Static wiring

The dedicated workflow verifies app-shell and schema versions, script and stylesheet wiring, service-worker assets, syntax, documentation, static deployment boundaries, and absence of background synchronization handlers.

## Full regression expectation

Before merge, existing provider, recovery, custody, signing, synchronization, transfer, acceptance, rollback, panel-registry, mobile, review, and multi-browser verification workflows must remain green.
